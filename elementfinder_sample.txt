[[ .result, property1, property2          // isolates matching elements into array
    property3

    <|any order|subset>         // contents below must match every item in the right order, unless these are specified

    [ 'first' ]         // doesn't need to include the properties/selectors for the array, automatically matches the first array item
    [ 'second' ]

    1-3               // counter, N, N-, N+, N-M (0-M, 1-M, etc.), exactly 1 if omitted
    [ 'third' ]
]]

[ .result, property1, property2
    property3

    <|any order>     // automatically a subset

    [ child elements in order ]     // no counters here, if there's one [] it could match the parent
    [ child elements in order ]
    [ child elements in order ]
    [[ a bunch of child elements in order ]]
    [ child elements in order ]
]




Verify page contains [
    page title 'something', page url 'relative or absolute'     // these are just properties (function properties that return true on the body element)
]




Verify .elem contains 'text':
    [[.elem
        ['text']
    ]]

Verify every .elem contains 'text':
    [[.elem
        0+
        ['text']
    ]]
