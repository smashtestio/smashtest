getAll() {
    i('ElementFinder', '../../packages/js/elementfinder.js');
}

    open chrome
    open firefox

        open test page

            - normal EFs

                - one line

                    - text

                        finds elements based on innerText {
                            let ef = new ElementFinder(`'one'`);
                            let results = await ef.getAll(browser.driver);

                            results.ef = ElementFinder.parseObj(results.ef);
                            expect(results.ef.hasErrors()).to.be.false;
                            expect(results.matches).to.have.lengthOf.at.least(4);
                        }

                        $s finds elements based on innerText, where the matching text is of a different case and has differing whitespace {
                            // Containing, lower case, trimmed, and whitespace-to-single-space match
                        }

                        $s finds elements based on value {

                        }

                        $s finds elements based on placeholder {

                        }

                        $s finds elements based on an associated label's innerText {

                        }

                        $s doesn't find elements based on text {
                            // verify error message with -->'s
                        }

                        $s finds elements based on text that spans multiple elements {

                        }

                    - defined prop



                    - selector $s
                    - ord $s
                    - counter $s
                    - match me $s
                    - multiple props $s
                    - visible prop $s
                    - errors $s

                - one child $s
                - one level of children $s
                - multiple levels of children $s

            - element array EFs $s

* open test page {
    await browser.nav(`file:///${dir()}/generic-page.html`);
}
