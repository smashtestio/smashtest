// ***************************************
//  Hooks
// ***************************************

*** Before Every Branch {
    let path = i('path');
    let HttpApi = i('HttpApi', `${path.dirname(require.main.filename)}/packages/js/httpapi.js`);
    runInstance.g('api', new HttpApi(runInstance));
}
